{
  "name": "ada-deposit-withdraw",
  "version": "1.0.0",
  "type": "module",
  "description": "A Cardano smart contract that allows users to deposit ADA into the contract and only the owner of the contract can withdraw it",
  "scripts": {
    "generate-credentials": "tsc && node dist/generate-credentials.js",
    "lock": "tsc && node dist/lock.js",
    "unlock": "tsc && node dist/unlock.js",
    "build": "tsc",
    "test": "mocha"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^4.3.12",
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.11.25",
    "chai": "^5.1.0",
    "mocha": "^10.3.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.2"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "inquirer": "^9.2.15",
    "lucid-cardano": "0.8.3"
  }
}
